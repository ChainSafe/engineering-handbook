name: Documentation

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build docs
      run: sh ./build-docs.sh

    - name: Publish docs
      run: sh ./publish-docs.sh
